subprojects{
    apply plugin: "groovy"
    apply plugin: "idea"
    apply plugin: "eclipse"

    repositories {
        jcenter()
    }

    defaultTasks "clean", "build"
}

defaultTasks "clean", "build"